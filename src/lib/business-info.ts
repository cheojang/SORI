// 전자상거래법 제10조 필수 표시 사항 — 단일 소스.
//
// 랜딩 푸터·이용약관·개인정보처리방침 세 곳에 같은 값이 노출되어야 한다.
// NHN KCP 카드사 등록심사에서 "홈페이지 메인 하단 사업자 필수정보"를 확인하며,
// 심사 통과 후 이 정보가 바뀌면 가맹이 취소될 수 있으므로 값을 흩어두지 않는다.

export const SERVICE_NAME = "바른발음";

/** 사업자등록증상 상호 — 서비스 브랜드명(바른발음)과 별개 */
export const COMPANY_NAME = "티엔피";

export const CEO_NAME = "유태봉";
export const BIZ_REG_NUMBER = "269-09-03462";
export const BIZ_ADDRESS = "경기도 성남시 분당구 미금로 184, 103동 403호(구미동, 까치마을)";

/**
 * 유선전화번호 — 카드사 심사 필수 항목.
 * 070 인터넷전화 번호를 발급받는 대로 이 값만 채우면 세 화면에 동시에 반영된다.
 * 빈 문자열이면 표시 자체를 생략한다(잘못된 값을 노출하는 것보다 낫다).
 */
export const BIZ_PHONE = "";

/**
 * 통신판매업 신고번호.
 * 직전년도 거래 50회 미만이거나 간이과세자면 신고 면제 대상이라 값이 없을 수 있다.
 * 면제 사업자는 미표기가 정상이며, KCP 심사 체크리스트도 이를 예외로 인정한다.
 */
export const MAIL_ORDER_REG_NUMBER = "";
